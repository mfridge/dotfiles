" Make colors visible on dark background terminal
set background=dark

" Show line numbers as relativenumbers
set number relativenumber

" Enable syntax highlighting
syntax on

" Enable automatic indentation
set autoindent

" Set tabstop to 4 characters
set tabstop=4

" Replace tabs with spaces
set expandtab

" Remove 4 spaces on backspace
set softtabstop=4

